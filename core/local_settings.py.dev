import os

SECRET_KEY = 'secret_key(required)'

EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_USE_TLS = True
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_HOST_USER = 'gmail_address(required)'
EMAIL_HOST_PASSWORD = 'gmail_app_password(required)'